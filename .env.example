# Database configuration
# Your Neon database connection URL. Example:
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require&channel_binding=require

# Authentication configuration
# JWT secret key for signing tokens.
# This value is mandatory: set a strong passphrase.
JWT_SECRET=your-secure-passphrase

# Web Authentication (WebAuthn/Passkeys)
# Comma-separated list of allowed origins. Supports wildcards.
# Example: https://*.vercel.app
WEBAUTHN_ORIGINS="https://*.vercel.app"

# AI/LLM
OPENAI_API_KEY=sk-...
OPENAI_BASE_URL=https://opencode.ai/zen/v1

# OAuth
# Base URL of the OAuth application
# Redirect URIs authorized for OAuth callbacks
OAUTH_APP_BASE_URL=https://your-project-name.vercel.app/
# For clients like MCP inspector, you may want to include the localhost redirect URI as well:
# OAUTH_ALLOWED_REDIRECT_URIS=http://localhost:6274/oauth/callback,http://localhost:6274/oauth/callback/debug
